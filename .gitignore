# Archivos de estado y configuración local de Terraform
# ¡NUNCA subas tu archivo de estado a un repositorio público!
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
.terraform.lock.hcl
.terraform.tfstate.lock.info

# Archivos generados por el empaquetado del código
# El .zip se genera cada vez que despliegas, no necesita estar en el repo.
app.zip
deployment_package.zip

# Dependencias de Node.js
# La carpeta node_modules puede tener miles de archivos y se instala con 'npm install'
app/node_modules/
node_modules/

# Archivos de log y debug de Node.js
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos del sistema operativo
.DS_Store
Thumbs.db
